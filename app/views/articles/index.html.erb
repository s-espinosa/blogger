<h1>Articles</h1>
<% @articles.each do |article| %>
  <p><%= link_to article.title, article_path(article.id), class: 'article_title' %></p>
<% end %><br>
<%= link_to "New Article", new_article_path, class: 'new_article'%>
